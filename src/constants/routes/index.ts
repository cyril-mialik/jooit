import { NAMES as ROUTE_NAMES } from './names'
import { PATHS as ROUTE_PATHS } from './paths'
import { DEFAULT_TRANSTION as META_DEFAULT_TRANSITION } from './meta'

export const PATHS = ROUTE_PATHS
export const NAMES = ROUTE_NAMES

export const DEFAULT_TRANSITION = META_DEFAULT_TRANSITION

