import { MENU as GLOBAL_MENU } from '@/constants/menu'
import {
  TOP_COMPONENTS as GLOBAL_TOP_COMPONENTS,
  NAVIGATION as GLOBAL_NAVIGATION ,
  EMPTY as GLOBAL_EMPTY
} from '@/constants/components'

export const TOP_COMPONENTS = GLOBAL_TOP_COMPONENTS
export const MENU = GLOBAL_MENU
export const NAVIGATION = GLOBAL_NAVIGATION
export const EMPTY = GLOBAL_EMPTY

